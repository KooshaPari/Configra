name: self-merge-gate
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


# Delegates to phenoShared reusable workflow.
# See: https://github.com/KooshaPari/phenoShared/blob/main/.github/workflows/reusable/self-merge-gate.yml

on:
  pull_request_review:
  timeout-minutes: 10

jobs:
  gate:
    uses: KooshaPari/phenoShared/.github/workflows/self-merge-gate.yml@136def14b63e59da150eae1dc525677746098dd0
