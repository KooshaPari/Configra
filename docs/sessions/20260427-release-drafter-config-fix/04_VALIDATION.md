# Validation

## Expected checks

- `.github/release-drafter.yml` exists.
- The workflow wrapper still points at the reusable Release Drafter workflow.
- The config file uses the default location expected by the action docs.
